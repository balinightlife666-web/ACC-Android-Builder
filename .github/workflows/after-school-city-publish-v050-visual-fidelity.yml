# Owner-approved AFTER SCHOOL CITY Visual Fidelity v0.5 LIVE publish
name: After School City Publish Visual Fidelity V0.5

on:
  push:
    branches: [main]
    paths: ['.github/workflows/after-school-city-publish-v050-visual-fidelity.yml']
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: after-school-city-live-publish
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      SOURCE_REPO: ardarawk-cloud/ACC-Roblox-maps
      SOURCE_SHA: ca68820c3833140ff6ff4552fcbc90a8914e278a
      UNIVERSE_ID: '10745359869'
      PLACE_ID: '121603385909425'
      ROBLOX_API_KEY: ${{ secrets.ROCKET_RACOON_PUBLISH }}
    steps:
      - name: Checkout publisher repository
        uses: actions/checkout@v4
        with: {fetch-depth: 0}

      - name: Checkout locked After School City source
        uses: actions/checkout@v4
        with:
          repository: ardarawk-cloud/ACC-Roblox-maps
          ref: ca68820c3833140ff6ff4552fcbc90a8914e278a
          token: ${{ secrets.ACC_ROBLOX_MAPS_WRITE_TOKEN }}
          path: asc-source
          fetch-depth: 1

      - name: Preflight exact Visual Fidelity v0.5
        shell: bash
        run: |
          set -euo pipefail
          [[ -n "${ROBLOX_API_KEY:-}" ]] || { echo 'ROCKET_RACOON_PUBLISH missing'; exit 10; }
          actual=$(git -C asc-source rev-parse HEAD)
          [[ "$actual" == "$SOURCE_SHA" ]] || { echo "Source SHA mismatch: $actual"; exit 11; }
          project='asc-source/maps/after-school-city/default.project.json'
          intro='asc-source/maps/after-school-city/after-school-city.intro.client.lua'
          config='asc-source/maps/after-school-city/after-school-city.config.lua'
          visual='asc-source/maps/after-school-city/after-school-city.visual-fidelity.server.lua'
          jq -e . "$project" >/dev/null
          for token in 'ASC_OrientationCorrection' 'ASC_VisualFidelity' 'after-school-city.visual-fidelity.server.lua'; do grep -Fq "$token" "$project"; done
          for token in 'PUTU AZYA PUTRI' 'BINTANG HARDAJAYA' 'WITH LOVE, DAD' 'ENTER THE CITY'; do grep -Fq "$token" "$intro"; done
          for token in '0.5.0-visual-fidelity-1' 'EnableOrientationCorrectionPass = true' 'EnableVisualFidelityPass = true'; do grep -Fq "$token" "$config"; done
          for token in 'V050_VisualFidelity' 'ASC_DowntownInteriorWidthReconciled' 'ASC_StudentRowDoorTreesCleared' 'ASC_SkateSignCompacted' 'ASC_ExteriorLightsNormalized'; do grep -Fq "$token" "$visual"; done
          if grep -Eq 'MarketplaceService|PromptProductPurchase|ProcessReceipt|DataStoreService|BillboardGui' "$visual"; then
            echo 'Forbidden authority/debug marker in V0.5 visual layer'
            exit 12
          fi
          echo "LOCK PASS source=$actual visual_v0_5=PASS dedication=PASS"

      - name: Cloud source spatial audit V4
        shell: pwsh
        working-directory: asc-source
        run: |
          $ErrorActionPreference='Stop'
          & '.github/scripts/after-school-city-spatial-audit.ps1'

      - name: Cloud source orientation audit V1
        shell: pwsh
        working-directory: asc-source
        run: |
          $ErrorActionPreference='Stop'
          & '.github/scripts/after-school-city-orientation-audit.ps1'

      - name: Cloud source visual fidelity audit V1
        shell: pwsh
        working-directory: asc-source
        run: |
          $ErrorActionPreference='Stop'
          & '.github/scripts/after-school-city-visual-fidelity-audit.ps1'

      - name: Prepare Rojo 7.7.0
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/asc-rojo"
          curl -fsSL 'https://github.com/rojo-rbx/rojo/releases/download/v7.7.0/rojo-7.7.0-linux-x86_64.zip' -o "$RUNNER_TEMP/rojo.zip"
          unzip -q -o "$RUNNER_TEMP/rojo.zip" -d "$RUNNER_TEMP/asc-rojo"
          rojo=$(find "$RUNNER_TEMP/asc-rojo" -type f -name rojo | head -n1)
          [[ -n "$rojo" ]] || exit 20
          chmod +x "$rojo"
          version=$("$rojo" --version)
          echo "$version"
          [[ "$version" == *'7.7.0'* ]] || { echo "Wrong Rojo: $version"; exit 21; }
          echo "ROJO_EXE=$rojo" >> "$GITHUB_ENV"

      - name: Build exact After School City place
        shell: bash
        run: |
          set -euo pipefail
          place="$RUNNER_TEMP/after-school-city-v050.rbxl"
          "$ROJO_EXE" build 'asc-source/maps/after-school-city/default.project.json' -o "$place"
          test -f "$place"
          bytes=$(stat -c%s "$place"); (( bytes >= 1024 ))
          sha=$(sha256sum "$place" | awk '{print $1}')
          echo "PLACE_FILE=$place" >> "$GITHUB_ENV"
          echo "PLACE_BYTES=$bytes" >> "$GITHUB_ENV"
          echo "PLACE_SHA256=$sha" >> "$GITHUB_ENV"
          echo "BUILD PASS bytes=$bytes sha256=$sha"

      - name: Publish Visual Fidelity v0.5 LIVE to Roblox
        shell: bash
        run: |
          set -euo pipefail
          url="https://apis.roblox.com/universes/v1/${UNIVERSE_ID}/places/${PLACE_ID}/versions?versionType=Published"
          http=$(curl -sS --location -o "$RUNNER_TEMP/asc-publish-response.json" -w '%{http_code}' -X POST "$url" -H "x-api-key: $ROBLOX_API_KEY" -H 'Content-Type: application/octet-stream' --data-binary "@$PLACE_FILE")
          body=$(cat "$RUNNER_TEMP/asc-publish-response.json")
          [[ "$http" =~ ^2[0-9][0-9]$ ]] || { echo "$body"; exit 30; }
          version=$(jq -r '.versionNumber // empty' "$RUNNER_TEMP/asc-publish-response.json")
          [[ -n "$version" ]] || { echo "Missing versionNumber: $body"; exit 31; }
          echo "PUBLISHED_VERSION=$version" >> "$GITHUB_ENV"
          echo "ROBLOX PUBLISH PASS version=$version"

      - name: Persist LIVE receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p deploy-status
          jq -n --arg status 'LIVE_PUBLISHED' --arg project 'AFTER SCHOOL CITY' --arg mode 'VISUAL_FIDELITY_V0_5' --arg sourceRepo "$SOURCE_REPO" --arg sourceCommit "$SOURCE_SHA" --arg universeId "$UNIVERSE_ID" --arg placeId "$PLACE_ID" --arg versionNumber "$PUBLISHED_VERSION" --arg rojoVersion '7.7.0' --arg placeBytes "$PLACE_BYTES" --arg placeSha256 "$PLACE_SHA256" --arg runId "$GITHUB_RUN_ID" --arg publishedAt "$(date -u +%Y-%m-%dT%H:%M:%SZ)" '{status:$status,project:$project,mode:$mode,sourceRepo:$sourceRepo,sourceCommit:$sourceCommit,universeId:$universeId,placeId:$placeId,versionNumber:($versionNumber|tonumber),rojoVersion:$rojoVersion,placeBytes:($placeBytes|tonumber),placeSha256:$placeSha256,runId:$runId,publishedAt:$publishedAt,note:"Visual Fidelity v0.5; exact locked source; Cloud Spatial V4 + Orientation V1 + Visual Fidelity V1 gates passed; dedication intro preserved."}' > deploy-status/after-school-city-publish-visual-fidelity-v0-5.json
          cat deploy-status/after-school-city-publish-visual-fidelity-v0-5.json
          git config user.name 'After School City Publish Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add deploy-status/after-school-city-publish-visual-fidelity-v0-5.json
          git commit -m "Record After School City Visual Fidelity publish v${PUBLISHED_VERSION} [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main
