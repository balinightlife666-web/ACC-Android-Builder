name: Rocket Racoon Publish Key Test

on:
  workflow_dispatch:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write

jobs:
  test-key:
    if: github.event_name == 'workflow_dispatch' || startsWith(github.event.issue.title, 'ROCKET RACOON PUBLISH KEY TEST')
    runs-on: ubuntu-latest
    env:
      ROBLOX_KEY: ${{ secrets.ROCKET_RACOON_PUBLISH }}
    steps:
      - name: Introspect RR Roblox Open Cloud key
        id: keycheck
        shell: bash
        run: |
          set +e
          if [ -z "$ROBLOX_KEY" ]; then
            result="SECRET_MISSING"
          else
            tmp=$(mktemp)
            code=$(curl -sS -o "$tmp" -w '%{http_code}' -X POST 'https://apis.roblox.com/api-keys/v1/introspect' \
              -H 'Content-Type: application/json' \
              --data "{\"apiKey\":\"$ROBLOX_KEY\"}")
            if [ "$code" != "200" ]; then
              result="INTROSPECT_HTTP_$code"
            else
              response=$(cat "$tmp")
              result=$(python3 - "$response" <<'PY'
          import json,sys
          try:
              d=json.loads(sys.argv[1])
              uid=str(d.get('authorizedUserId') or '')
              scopes=d.get('scopes') or []
              scope_summary=[]
              for s in scopes:
                  name=s.get('name') or '?'
                  ops=','.join(s.get('operations') or [])
                  scope_summary.append(f'{name}[{ops}]')
              if d.get('expired') is True:
                  print(f'KEY_EXPIRED owner={uid}')
              elif d.get('enabled') is False:
                  print(f'KEY_DISABLED owner={uid}')
              else:
                  print(f'PASS owner={uid} scopes={";".join(scope_summary)}')
          except Exception:
              print('INVALID_INTROSPECT_RESPONSE')
          PY
          )
            fi
            rm -f "$tmp"
          fi
          echo "result=$result" >> "$GITHUB_OUTPUT"
          echo "$result"

      - name: Comment result on test issue
        if: github.event_name == 'issues'
        env:
          GH_TOKEN: ${{ github.token }}
          RESULT: ${{ steps.keycheck.outputs.result }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          REPO: ${{ github.repository }}
        run: gh issue comment "$ISSUE_NUMBER" --repo "$REPO" --body "$RESULT"

  publish-asc-premium-v02:
    if: github.event_name == 'issues' && startsWith(github.event.issue.title, 'ASC PREMIUM V02 LIVE')
    runs-on: ubuntu-latest
    timeout-minutes: 15
    concurrency:
      group: after-school-city-live-publish
      cancel-in-progress: false
    env:
      SOURCE_REPO: ardarawk-cloud/ACC-Roblox-maps
      SOURCE_SHA: 73d6c8151da2f3be724f454ec21f0f5ab225aece
      UNIVERSE_ID: '10745359869'
      PLACE_ID: '121603385909425'
      ROBLOX_API_KEY: ${{ secrets.ROCKET_RACOON_PUBLISH }}

    steps:
      - name: Checkout publisher repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Checkout locked After School City source
        uses: actions/checkout@v4
        with:
          repository: ardarawk-cloud/ACC-Roblox-maps
          ref: 73d6c8151da2f3be724f454ec21f0f5ab225aece
          token: ${{ secrets.ACC_ROBLOX_MAPS_WRITE_TOKEN }}
          path: asc-source
          fetch-depth: 1

      - name: Verify Premium v0.2 source and target
        shell: bash
        run: |
          set -euo pipefail
          [[ -n "${ROBLOX_API_KEY:-}" ]] || { echo 'ROCKET_RACOON_PUBLISH missing'; exit 10; }
          actual="$(git -C asc-source rev-parse HEAD)"
          [[ "$actual" == "$SOURCE_SHA" ]] || { echo "Source mismatch: $actual"; exit 11; }
          project='asc-source/maps/after-school-city/default.project.json'
          intro='asc-source/maps/after-school-city/after-school-city.intro.client.lua'
          config='asc-source/maps/after-school-city/after-school-city.config.lua'
          world='asc-source/maps/after-school-city/after-school-city.world.server.lua'
          jq -e . "$project" >/dev/null
          grep -Fq '0.2.0-premium-pass-1' "$config"
          grep -Fq '10745359869' "$config"
          grep -Fq '121603385909425' "$config"
          grep -Fq 'dedication intro popup v1.1 compact' "$intro"
          grep -Fq 'PUTU AZYA PUTRI' "$intro"
          grep -Fq 'BINTANG HARDAJAYA' "$intro"
          grep -Fq 'WITH LOVE, DAD' "$intro"
          grep -Fq 'ENTER THE CITY' "$intro"
          grep -Fq 'SchoolDistrict' "$world"
          grep -Fq 'Downtown' "$world"
          grep -Fq 'SkatePark' "$world"
          grep -Fq 'SportsField' "$world"
          echo "LOCK PASS source=$actual universe=$UNIVERSE_ID place=$PLACE_ID"

      - name: Install Rojo 7.7.0
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "$RUNNER_TEMP/asc-rojo"
          curl -fsSL 'https://github.com/rojo-rbx/rojo/releases/download/v7.7.0/rojo-7.7.0-linux-x86_64.zip' -o "$RUNNER_TEMP/rojo.zip"
          unzip -q -o "$RUNNER_TEMP/rojo.zip" -d "$RUNNER_TEMP/asc-rojo"
          rojo="$(find "$RUNNER_TEMP/asc-rojo" -type f -name rojo | head -n1)"
          [[ -n "$rojo" ]] || { echo 'Rojo binary missing'; exit 20; }
          chmod +x "$rojo"
          "$rojo" --version
          echo "ROJO_EXE=$rojo" >> "$GITHUB_ENV"

      - name: Build exact place
        shell: bash
        run: |
          set -euo pipefail
          place="$RUNNER_TEMP/after-school-city.rbxl"
          "$ROJO_EXE" build 'asc-source/maps/after-school-city/default.project.json' -o "$place"
          test -f "$place"
          bytes="$(stat -c%s "$place")"
          (( bytes >= 1024 )) || { echo "Generated place too small: $bytes"; exit 21; }
          sha="$(sha256sum "$place" | awk '{print $1}')"
          echo "PLACE_FILE=$place" >> "$GITHUB_ENV"
          echo "PLACE_BYTES=$bytes" >> "$GITHUB_ENV"
          echo "PLACE_SHA256=$sha" >> "$GITHUB_ENV"
          echo "BUILD PASS bytes=$bytes sha256=$sha"

      - name: Publish Premium Foundation v0.2 LIVE
        shell: bash
        run: |
          set -euo pipefail
          url="https://apis.roblox.com/universes/v1/${UNIVERSE_ID}/places/${PLACE_ID}/versions?versionType=Published"
          http="$(curl -sS --location -o "$RUNNER_TEMP/publish.json" -w '%{http_code}' \
            -X POST "$url" \
            -H "x-api-key: $ROBLOX_API_KEY" \
            -H 'Content-Type: application/octet-stream' \
            --data-binary "@$PLACE_FILE")"
          echo "Roblox publish HTTP $http"
          cat "$RUNNER_TEMP/publish.json"
          [[ "$http" =~ ^2[0-9][0-9]$ ]] || exit 30
          version="$(jq -r '.versionNumber // empty' "$RUNNER_TEMP/publish.json")"
          [[ -n "$version" ]] || exit 31
          echo "PUBLISHED_VERSION=$version" >> "$GITHUB_ENV"
          echo "ROBLOX PUBLISH PASS version=$version"

      - name: Persist LIVE receipt
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p deploy-status
          jq -n \
            --arg status 'LIVE_PUBLISHED' \
            --arg project 'AFTER SCHOOL CITY' \
            --arg mode 'PREMIUM_FOUNDATION_V0_2' \
            --arg sourceRepo "$SOURCE_REPO" \
            --arg sourceCommit "$SOURCE_SHA" \
            --arg universeId "$UNIVERSE_ID" \
            --arg placeId "$PLACE_ID" \
            --arg versionNumber "$PUBLISHED_VERSION" \
            --arg rojoVersion '7.7.0' \
            --arg placeBytes "$PLACE_BYTES" \
            --arg placeSha256 "$PLACE_SHA256" \
            --arg runId "$GITHUB_RUN_ID" \
            --arg publishedAt "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
            '{status:$status,project:$project,mode:$mode,sourceRepo:$sourceRepo,sourceCommit:$sourceCommit,universeId:$universeId,placeId:$placeId,versionNumber:($versionNumber|tonumber),rojoVersion:$rojoVersion,placeBytes:($placeBytes|tonumber),placeSha256:$placeSha256,runId:$runId,publishedAt:$publishedAt,note:"Premium Foundation v0.2 plus compact dedication intro."}' \
            > deploy-status/after-school-city-publish-premium-v0-2.json
          cat deploy-status/after-school-city-publish-premium-v0-2.json
          git config user.name 'After School City Publish Bot'
          git config user.email 'actions@users.noreply.github.com'
          git add deploy-status/after-school-city-publish-premium-v0-2.json
          git commit -m "Record ASC Premium v0.2 publish v${PUBLISHED_VERSION} [skip ci]"
          git pull --rebase origin main
          git push origin HEAD:main

      - name: Comment LIVE receipt
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
          ISSUE_NUMBER: ${{ github.event.issue.number }}
          REPO: ${{ github.repository }}
        run: |
          gh issue comment "$ISSUE_NUMBER" --repo "$REPO" --body "PASS — AFTER SCHOOL CITY Premium Foundation v0.2 LIVE. Roblox version ${PUBLISHED_VERSION}; source ${SOURCE_SHA}; Universe ${UNIVERSE_ID}; Place ${PLACE_ID}."
