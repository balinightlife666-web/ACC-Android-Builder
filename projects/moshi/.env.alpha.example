# Copy to .env.alpha locally. Never commit the real file.
MOSHI_POSTGRES_PASSWORD=CHANGE_ME_LONG_RANDOM_PASSWORD
MOSHI_JWT_SECRET=CHANGE_ME_AT_LEAST_32_RANDOM_CHARACTERS

# Optional until Firebase is provisioned.
MOSHI_FCM_PROJECT_ID=
